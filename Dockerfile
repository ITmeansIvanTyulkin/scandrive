# Используем официальный Maven-образ с JDK
FROM maven:3.9-eclipse-temurin-17

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем исходники проекта в образ
COPY . /app

# Указываем команду по умолчанию: запуск Maven с аргументами, переданными при запуске контейнера
ENTRYPOINT ["mvn"]